view {
  contain.text {
    name = "text similar to: {0}"
    selector = "{1}//*[contains(text(),'{0}')]"
    exactly {
      name = "the text exactly: {0}"
      selector = "{1}//*[text()[normalize-space()='{0}']]"
    }
  }
}


